name: Deep audit source export

on:
  push:
    branches:
      - audit/deep-ownership-pass

permissions:
  contents: read

jobs:
  export:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Package repository source
        run: |
          zip -qr roth-tooltip-audit-source.zip . \
            -x '.git/*' \
            -x 'roth-tooltip-audit-source.zip'
      - uses: actions/upload-artifact@v4
        with:
          name: roth-tooltip-audit-source
          path: roth-tooltip-audit-source.zip
          retention-days: 1
          if-no-files-found: error
